MODE=code # replace <MODE> below with that mode name

Твоя задача: проверить, что спецификация к режиму-агенту <MODE> позволяет сгенерировать (воссоздать) такой же системный промпт который сейчас уже есть в файле `.roo/system-prompt-<MODE>`. Другими словами, спецификация должна предлагать полный спектр инструкций, правил и объяснений, необходимых для создания системного промпта, который будет идентичен тому, что уже существует в файле `.roo/system-prompt-<MODE>`.

Терминология:
- системный промпт – это файл `.roo/system-prompt-<MODE>`;
- спецификация – это файл `<MODE>-MODE.md`.

С чего начать:

- В файле README.md расположено общее описание многоагентной системы которую мы разрабатываем;

- В файле SYSTEM-PROMPT.md расположены требования и гайды по написанию системного промпта для режима-агента;

- В файле <MODE>-MODE.md находится спецификация к режиму-агенту <MODE>.

Что нужно сделать:

- проанализируй спецификацию к режиму-агенту <MODE> и проверь, что она позволяет сгенерировать (воссоздать) такой же системный промпт который сейчас уже есть в файле `.roo/system-prompt-<MODE>`

- если в спецификации не объясняется её предназначение (генерация системного промпта), то нужно добавить это в спецификацию;

- если спецификация не написана в формате инструкции в императивном наклонении, то нужно переписать её в формате инструкции в императивном наклонении;

- если спецификация недостаточно полная, недостаточно подробно освещает что-то что есть в системном промпте, то нужно собрать список несоответствий и недочетов, которые нужно исправить в спецификации;

- если спецификация не диктует, как именно должен выглядеть системный промпт, а только описывает его общие черты, то нужно собрать список несоответствий и недочетов, которые нужно исправить в спецификации;

- если спецификация не фиксирует структуру системного промпта, то нужно учесть это и записать в список несоответствий и недочетов, которые нужно исправить в спецификации;

- если несоответствия выявлены или уверенности, что системный промпт можно будет воссоздать по этой спецификации, нет, то нужно написать предложить правки в спецификацию, одну за другой, пока уверенность не достигнет 10 пунктов из 10.

Что обязательно должна содержать спецификация:
- первый раздел "## Purpose" с описанием назначения спецификации;
- второй раздел "## Specification" с таблицей, в которой указаны атрибуты системного промпта и их значения (обрати внимание, что максимальная длина строки в таблице должна быть 80 символов).

Пример Specification таблицы:
```markdown
| **Attribute**       | **Details**                                                    |
|---------------------|----------------------------------------------------------------|
|                     |                                                                |
| **ID**              | mode-id                                                        |
|                     |                                                                |
| **Description**     | Describe for what the mode is responsible here, pay attension  |
|                     | that's not a basic markdown table, but a table with            |
|                     | multiple lines in one cell. That cell is an example of that.   |
|                     |                                                                |
| **System Prompt**   | .roo/system-prompt-<MODE>                                      |
|                     |                                                                |
| **Available Tools** | a list of available tools for the mode (delimited by commas)   |
|                     |                                                                |
| **MCP Servers**     | a list of available MCP servers for the mode (delimited        |
|                     | by commas)                                                     |
|                     |                                                                |
| **Spawnable Modes** | which modes might be spawned by <MODE> via <new_task> tool     |
|                     |                                                                |
```

На что нужно обратить внимание отдельно:
- на список инструментов, которые есть в системном промпте и которые должны быть в спецификации;
- на доступные MCP сервера;
- на то описано ли какие режимы могут быть пораждены в этом режиме;
- что спецификация не предполагает, что системный промпт уже создан и в него можно "подсмотреть" и взять оттуда информацию;
- implementation guidelines MUST BE specific and comprehensive in higher degree of detail.

Что нужно учитывать:
- it's allowed to refer to `SYSTEM-PROMPTS.md` for general principles and best practices, but ensure all specific content should be incorporated in the specification.
- specification must not reference an existing system prompt directly;
- instructions in specification cannot assume the existence of and access to the system prompt file that we're trying to create;
- specification to be completely self-contained and independent of the system prompt file, only relying on the information provided by SYSTEM-PROMPT.md and README.md is acceptable;
- не стоит дублировать ту информацию, которая уже есть в README.md или SYSTEM-PROMPT.md, но отсутствует в спецификации;
- если что-то выглядит как релевантное для любых режимов, то нужно предложить добавить это в README.md или SYSTEM-PROMPT.md, а не в спецификацию к режиму-агенту <MODE>, чтобы пошарить это между всеми режимами-агентами;
- тебе не разрешается вносить в файле саморефлексию вида: 
    a. "self-correction", "self-criticism", "self-reflection" и т.д.;
    b. "*(This section now more closely matches the system prompt's examples)*"
    c. any other kind of self-reflection;
- нет необходимости держать в спецификации весь текст системного промпта (одним куском или декомпозировано) с точностью до символа, но нужно сделать так, чтобы спецификация позволяла сгенерировать (воссоздать) такой же системный промпт, как в файле `.roo/system-prompt-<MODE>` руководствуясь правилами или инструкциями из спецификации;
- спецификации для режимов ОЧЕНЬ редко бывают меньше 300 строк, поэтому если ты видишь, что спецификация меньше 300 строк, то это повод задуматься и проверить, что там все в порядке;
- нельзя вносить в спецификацию инструкции вида "Include these exact rules: <long exact quote>" или "Include these exact examples: <long exact quote>", спецификация ДОЛЖНА разъяснять, как именно нужно сформулировать правила и примеры, а не просто вставлять их в текст спецификации.

Что значит воссоздать системный промпт?
- это значит, что при передаче в LLM файлов: README.md, SYSTEM-PROMPT.md и <MODE>-MODE.md, LLM должен сгенерировать такой же системный промпт, как в файле `.roo/system-prompt-<MODE>` не упустив никаких деталей и не добавив ничего лишнего.

Что я ожидаю получить:
1. список несоответствий и недочетов (list of gaps) в спецификации к режиму-агенту <MODE> по сравнению с системным промптом, который уже есть в файле `.roo/system-prompt-<MODE>`;
2. список правок в спецификацию к режиму-агенту <MODE> (list of changes) которые нужно внести в спецификацию, чтобы она позволяла сгенерировать (воссоздать) такой же системный промпт который сейчас уже есть в файле `.roo/system-prompt-<MODE>`;
3. !!IMPORTANT!! A complete rewrite of the file in a single code block. – предоставь все изменения единой правкой, которая бы перезаписала весь файл целиком, иначе редактирование не работает корректно.
4. четкий односложный ответ на вопрос: "Получится ли сгенерировать такой же системный промпт, по спецификации после этих правок?", а также степень твоей уверенности в этом от 1 до 100, где 1 - это "нет, не получится", а 100 - это "да, получится". Если уверенность меньше 100, то нужно указать, что именно нужно будет уточнить у меня, чтобы повысить уверенность до 100.

**НИ В КОЕМ СЛУЧАЕ** не нужно вносить правки в файлы README.md, SYSTEM-PROMPT.md или system-prompt-<MODE>. Тебе разрешается вносить правки только в файл <MODE>-MODE.md.

Вноси изменения в файл <MODE>-MODE.md одним куском (одним редактированием), а не по частям. Не нужно вносить правки в файл <MODE>-MODE.md по частям, так как это может привести к путанице и ошибкам.

После того, как ты все это сделаешь, нужно будет второй раз перепроверить файл <MODE>-MODE.md, чтобы убедиться, что текст не был corrupted в процессе редактирования.

P.S. Будь педантичен, внимателен и аккуратен. Если ты столкнешься с неоднозначностью, то нужно будет уточнить у меня, как именно это должно быть.