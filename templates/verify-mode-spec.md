MODE=new-task # replace <MODE> below with that mode name

Твоя задача: проверить, что спецификация к режиму-агенту <MODE> позволяет сгенерировать (воссоздать) такой же системный промпт который сейчас уже есть в файле `.roo/system-prompt-<MODE>`.

Терминология:
- системный промпт – это файл `.roo/system-prompt-<MODE>`;
- спецификация – это файл `<MODE>-MODE.md`.

С чего начать:

- В файле README.md расположено общее описание многоагентной системы которую мы разрабатываем;

- В файле SYSTEM-PROMPT.md расположены требования и гайды по написанию системного промпта для режима-агента;

- В файле <MODE>-MODE.md находится спецификация к режиму-агенту <MODE>.

Что нужно сделать:

- проанализируй спецификацию к режиму-агенту <MODE> и проверь, что она позволяет сгенерировать (воссоздать) такой же системный промпт который сейчас уже есть в файле `.roo/system-prompt-<MODE>`

- если в спецификации не объясняется её предназначение (генерация системного промпта), то нужно добавить это в спецификацию;

- если спецификация не написана в формате инструкции в императивном наклонении, то нужно переписать её в формате инструкции в императивном наклонении;

- если спецификация недостаточно полная, недостаточно подробно освещает что-то что есть в системном промпте, то нужно собрать список несоответствий и недочетов, которые нужно исправить в спецификации;

- если спецификация не диктует, как именно должен выглядеть системный промпт, а только описывает его общие черты, то нужно собрать список несоответствий и недочетов, которые нужно исправить в спецификации;

- если спецификация не фиксирует структуру системного промпта, то нужно учесть это и записать в список несоответствий и недочетов, которые нужно исправить в спецификации;

- если несоответствия выявлены или уверенности, что системный промпт можно будет воссоздать по этой спецификации, нет, то нужно написать предложить правки в спецификацию, одну за другой, пока уверенность не достигнет 10 пунктов из 10.

На что нужно обратить внимание отдельно:
- на список инструментов, которые есть в системном промпте и которые должны быть в спецификации;
- на доступные MCP сервера;
- на то описано ли какие режимы могут быть пораждены в этом режиме.

Что нужно учитывать:
- не стоит дублировать ту информацию, которая уже есть в README.md или SYSTEM-PROMPT.md, но отсутствует в спецификации;
- если что-то выглядит как релевантное для любых режимов, то нужно предложить добавить это в README.md или SYSTEM-PROMPT.md, а не в спецификацию к режиму-агенту <MODE>, чтобы пошарить это между всеми режимами-агентами.

Что значит воссоздать системный промпт?
- это значит, что при передаче в LLM файлов: README.md, SYSTEM-PROMPT.md и <MODE>-MODE.md, LLM должен сгенерировать такой же системный промпт, как в файле `.roo/system-prompt-<MODE>` не упустив никаких деталей и не добавив ничего лишнего.

Что я ожидаю получить:
1. список несоответствий и недочетов (list of gaps) в спецификации к режиму-агенту <MODE> по сравнению с системным промптом, который уже есть в файле `.roo/system-prompt-<MODE>`;
2. список правок в спецификацию к режиму-агенту <MODE> (list of changes) которые нужно внести в спецификацию, чтобы она позволяла сгенерировать (воссоздать) такой же системный промпт который сейчас уже есть в файле `.roo/system-prompt-<MODE>`;
3. четкий односложный ответ на вопрос: "Получится ли сгенерировать такой же системный промпт, по спецификации после этих правок?", а также степень твоей уверенности в этом от 1 до 100, где 1 - это "нет, не получится", а 100 - это "да, получится". Если уверенность меньше 100, то нужно указать, что именно нужно будет уточнить у меня, чтобы повысить уверенность до 100.

После того, как ты все это сделаешь, нужно будет второй раз перепроверить файл <MODE>-MODE.md, чтобы убедиться, что текст не был corrupted в процессе редактирования.

P.S. Будь педантичен, внимателен и аккуратен. Если ты столкнешься с неоднозначностью, то нужно будет уточнить у меня, как именно это должно быть.